# ═══════════════════════════════════════════════════════════════
# Call Intelligence Engine — Phase 1 Dependencies
# تثبيت: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── معالجة الصوت الأساسية ────────────────────────────────────────
librosa>=0.10.1              # تحليل ميزات الصوت + قراءة الملفات
soundfile>=0.12.1            # قراءة/كتابة ملفات الصوت
pydub>=0.25.1                # تقطيع وتعديل الصوت (يحتاج ffmpeg مثبتاً على النظام)
numpy>=1.24.0
scipy>=1.11.0

# ── تحميل البيانات ──────────────────────────────────────────────
datasets>=2.16.0             # HuggingFace Datasets — لـ Common Voice
huggingface_hub>=0.20.0      # تنزيل النماذج والبيانات
requests>=2.31.0
tqdm>=4.66.0                 # شريط تقدّم

# ── العربية ─────────────────────────────────────────────────────
pyarabic>=0.6.15             # تطبيع وتشكيل النصوص العربية

# ── التحليل والتقارير ─────────────────────────────────────────────
pandas>=2.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0              # طباعة جداول جميلة في الطرفية

# ── التعلم الآلي (للتجميع، DER) ────────────────────────────────────
scikit-learn>=1.3.0          # AgglomerativeClustering, SpectralClustering

# ── شبكة المعرفة ──────────────────────────────────────────────────
networkx>=3.0                # رسم بياني في الذاكرة، تحليل شبكي

# ── الواجهة البرمجية (المرحلة 7) ─────────────────────────────────
fastapi>=0.110.0             # REST API
uvicorn[standard]>=0.27.0    # خادم ASGI
python-multipart>=0.0.9      # دعم file uploads
pydantic>=2.5.0              # validation
httpx>=0.27.0                # HTTP client للاختبارات

# ── تقييم التفريغ ────────────────────────────────────────────────
jiwer>=3.0.0                 # WER/CER (مرجعي، نوفّر بديل مدمج أيضاً)

# ── جودة الكود والاختبار ───────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.12.0
ruff>=0.1.9

# ── إعدادات وتسجيل ───────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
loguru>=0.7.2                # تسجيل أنيق وملوّن

# ═══════════════════════════════════════════════════════════════
# Optional — تبعيات اختيارية لميزات متقدمة
# ═══════════════════════════════════════════════════════════════

# Silero VAD (اختياري) — أدق من energy VAD لكن يحتاج 200MB+
# torch>=2.0.0

# SpeechBrain ECAPA-TDNN (اختياري) — بصمات صوتية أدق بكثير من MFCC
# torch>=2.0.0
# torchaudio>=2.0.0
# speechbrain>=1.0.0

# Neo4j (اختياري) — رسم معرفة دائم بدلاً من في الذاكرة
# neo4j>=5.0.0

# faster-whisper (موصى به للإنتاج) — تفريغ Whisper سريع
# faster-whisper>=1.0.0
